---
import Socials from "./Socials.astro";
const currentYear = new Date().getFullYear();
---

<footer class="mt-auto w-full px-4 sm:px-0">
  <div class="footer-wrapper">
    <Socials centered />
    <div class="copyright-wrapper">
      <span>Copyright &#169; {currentYear}</span>
      <span class="separator">&nbsp;|&nbsp;</span>
      <span>All rights reserved</span>
    </div>
  </div>
</footer>

<style>
  .footer-wrapper {
    @apply flex flex-col items-center justify-between border-t border-theme-line py-4 sm:flex-row-reverse sm:py-4;
  }
  .link-button {
    @apply my-1 p-2 hover:rotate-6;
  }
  .link-button svg {
    @apply scale-125;
  }
  .copyright-wrapper {
    @apply my-2 flex flex-col items-center whitespace-nowrap sm:flex-row;
  }
  .separator {
    @apply hidden sm:inline;
  }
</style>
